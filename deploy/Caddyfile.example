# 反代示例（Caddy）。网关自己只监听 127.0.0.1，TLS 和域名都交给反代。
#
# 两条通道是**两个独立的网关实例**（独立端口 / 状态目录 / 密码 / 令牌），
# 各自一个域名，各自一条反代规则。

# shell 通道：网关 :18011 → supergateway :18010 → mcp-ssh-manager
host.example.com {
	reverse_proxy 127.0.0.1:18011
}

# 浏览器通道：网关 :18013 → Playwright MCP :18012 → CDP :9222 → 桌面上的 Chromium
browser.example.com {
	reverse_proxy 127.0.0.1:18013
}
